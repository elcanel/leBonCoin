

{{ form_start(form) }}
<div class="row">
    <div class="col-md-4">{{ form_row(form.mail) }}</div>
    <div class="col-md-4">{{ form_row(form.mdp) }}</div>
</div>

{{ form_rest(form) }}
<button class="btn btn-primary">{{ button|default('Connexion') }}</button>
{{ form_end(form) }}